<%= form_for @bulk_insert_list do |form| %>
  <% if bulk_insert_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bulk_insert_list.errors.count, "error") %> prohibited this bulk_insert_list from being
        saved:</h2>

      <ul>
        <% bulk_insert_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="display:none;">
    <%= form.label :hash_id %>
    <%= form.text_field :hash_id %>
  </div>

  <div class="field">
    <%= form.label :EAN13 %>
    <%= form.file_field :EAN13 %>
    <%= form.submit %>
  </div>
<% end %>
